declare module "https://cdn.jsdelivr.net/npm/@tscircuit/runframe@latest/+esm" {
  import type { ComponentType } from "react"

  export const CircuitJsonPreview: ComponentType<{
    circuitJson: unknown
  }>
}
